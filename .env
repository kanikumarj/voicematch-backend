# Server
PORT=4000
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173

# Database (URL-encoded password for Supabase)
DATABASE_URL=postgresql://postgres:%5B98127645%40Kk%5D@db.qlakxqujughtjxgdxzjt.supabase.co:5432/postgres
DB_POOL_MAX=10

# Redis
REDIS_URL=redis://localhost:6379

# Auth
JWT_SECRET=dev-super-secret-jwt-key-voicematch-2024-change-in-prod
JWT_EXPIRES_IN=7d

# TURN (disabled for local dev - direct P2P)
TURN_HOST=localhost
TURN_PORT=3478
TURN_SECRET=dev-turn-secret
TURN_CREDENTIAL_TTL=3600

# ─── Email — Multi-provider waterfall ────────────────────────────────────────
# Fill in ANY ONE of the providers below. The app tries them in order.
# You don't need all three — just whichever you have access to.

# Provider 1: Brevo (RECOMMENDED — free 300 emails/day, no domain needed)
# Sign up free at https://app.brevo.com
# Then: Account → SMTP & API → SMTP tab → copy login + key
BREVO_SMTP_USER=kanikumar.jp@gmail.com
BREVO_SMTP_KEY=8EZcvAfr2zaKnVhL

# Provider 2: Resend (free 100 emails/day — already have the API key)
# NOTE: Free plan only delivers to the email you signed up with
RESEND_API_KEY=re_NkWupSs9_PQnC6tqQMnhp6kyabJu8WkUT

# Provider 3: Gmail SMTP (requires App Password — may be blocked on college accounts)
SMTP_USER=kanikumarj.it2017@dscet.ac.in
SMTP_PASS=your_16_char_app_password_here
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465

# Sender identity (shown as From address + Reply-To on all emails)
EMAIL_FROM=kanikumarj.it2017@dscet.ac.in
APP_URL=http://localhost:5173

# Sentry (disabled for local dev)
SENTRY_DSN=
